name: firstworkflow-demo
on: push
jobs:
  learn-actions:
    runs-on: ubuntu-latest
    steps:
      - name: echo-string
        run: echo "Hello world"
      - name: Multiple-command
        run: | 
         node -v
         npm -v  
